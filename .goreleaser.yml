version: 2

project_name: wbts

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - main: ./cmd/wbts
    binary: wbts
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.ShortCommit}} -X main.date={{.Date}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE

nfpms:
  - package_name: wbts
    homepage: https://github.com/bruceowenga/wbts
    maintainer: Bruce Owenga <bruceowenga@gmail.com>
    description: |-
      wbts — what broke the server?
      Forensic incident timeline builder for Linux/Docker systems.
      Correlates journald, dmesg, Docker events, apt, auth, and cron logs
      into a single chronological timeline.
    license: MIT
    formats:
      - deb
      - rpm
    bindir: /usr/local/bin
    section: utils
    contents:
      - src: README.md
        dst: /usr/share/doc/wbts/README.md
        type: doc

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^chore:"
      - "^test:"
      - "^docs:"
      - "typo"
  groups:
    - title: Features
      regexp: "^feat:"
      order: 0
    - title: Bug Fixes
      regexp: "^fix:"
      order: 1
    - title: Other
      order: 999

brews:
  - name: wbts
    homepage: https://github.com/bruceowenga/wbts
    description: "What broke the server — forensic incident timeline for Linux/Docker"
    license: MIT
    repository:
      owner: bruceowenga
      name: homebrew-wbts
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    install: |
      bin.install "wbts"
    test: |
      system "#{bin}/wbts", "--version"

release:
  github:
    owner: bruceowenga
    name: wbts
  name_template: "v{{.Version}}"
  draft: false
  prerelease: auto
